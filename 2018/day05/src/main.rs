use std::{fs, iter::Enumerate};

fn main() {
    let file = fs::read_to_string("./src/sample").unwrap();
    
    
}

fn collapse(seq: &String) -> String {
    let new_string = String::new();

    seq.chars().enumerate().filter()

    new_string
}